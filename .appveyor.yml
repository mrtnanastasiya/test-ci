image: Ubuntu1804
stack: node 22
branches: 
  only:
    - main
cache: node_modules # кэширование
install: 
  - npm install # команда установки зависимостей
build: off
test_script:
  npm run lint && npm test